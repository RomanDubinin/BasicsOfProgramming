using System;
using System.Collections.Generic;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using TaskSchedule;

namespace Tests
{
    [TestClass]
    public class Tests
    {
        [TestMethod]
        public void TestMethod1()
        {
//            var tasks = new List<Task>
//            {
//                new Task {Start = 1, Duration = 2},
//                new Task {Start = 1, Duration = 2}
//            };
//            var scheduler = new TaskScheduler();
//            var actualShedule = scheduler.CreateSchedule(tasks);
//            var expectedShedule = new List<int> {0, 1, 0, 1};
//
//            CollectionAssert.AreEqual(expectedShedule, actualShedule);
        }
    }
}
